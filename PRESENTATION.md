# Object Detection & Removal - 발표 대본

## 1. 프로젝트 소개 및 제한 사항
   - 1.1. 프로젝트 목표: YOLO로 객체 탐지 후 GrabCut + Inpainting으로 객체 제거
   - 1.2. 제한 사항: 배경이 단조로운(monotonous) 이미지에서 가장 잘 동작함 (다양한 색상/패턴 배경은 어려움)

---

## 2. 수업 기법 기반 인페인팅 (Class Inpaint) - Best Case

### 2.1. 사용 기법 설명
- `erode()`: 마스크 경계를 1픽셀씩 찾아 안쪽으로 진행
- `subtract()`: 원본 마스크 - eroded 마스크 = 경계 픽셀
- 픽셀 접근 + 가중치 평균: 주변 알려진 픽셀들의 거리 기반 가중 평균 (weight = 1/distance²)
- `GaussianBlur()`: 최종 결과 경계 스무딩

### 2.2. 알고리즘 동작 원리
- **Step 1**: 마스크 경계에서 시작하여 안쪽으로 한 층씩 채워나감
- **Step 2**: 각 경계 픽셀을 주변 "알려진" 픽셀들의 가중 평균으로 계산
- **Step 3**: 채워진 픽셀은 "알려진" 상태가 되어 다음 층 계산에 사용됨
- **Step 4**: 마스크가 완전히 채워질 때까지 반복

### 2.3. 데모
- 단조로운 배경 (하늘, 벽 등)에서 좋은 결과 시연

---

## 3. 어려운 케이스 (Hard Cases)

### 3.1. 수업 기법의 한계점
- 복잡한 텍스처 배경에서 블러 현상 발생
- 큰 영역 제거 시 품질 저하
- 에지 방향성 고려하지 않음

### 3.2. 왜 한계가 있는가?
- 단순 거리 기반 가중 평균은 텍스처/패턴 정보를 보존하지 못함
- 경계에서 안쪽으로만 확산하므로 이미지 구조 정보 활용 불가

---

## 4. Telea 알고리즘 (직접 구현)

### 4.1. 더 좋은 알고리즘을 찾아서
- 수업 기법의 한계를 느끼고 논문을 찾아봄
- 2004년 Telea 논문 발견: "Fast Marching Method 기반 인페인팅"

### 4.2. 어떻게 작동하나?
- **Eikonal 방정식** 사용: 경계에서 안쪽으로 "파동"처럼 전파
- 픽셀 하나를 채울 때 **3가지 가중치**를 곱해서 최종 값 결정:
  - 거리 가중치: 가까울수록 영향력 큼
  - 방향 가중치: 에지 방향 보존
  - 레벨셋 가중치: 같은 층 픽셀 우선
- 결과: 수업 기법보다 **에지가 끊기지 않고** 자연스럽게 채워짐

---

## 5. LaMa 알고리즘 (딥러닝 기반)

### 5.1. LaMa란?
- "Large Mask Inpainting" - 2021년 발표된 딥러닝 인페인팅 모델
- 큰 영역도 자연스럽게 채울 수 있음

### 5.2. 알고리즘 핵심 원리

#### Fourier Convolution
- 주파수 도메인에서 컨볼루션 → 넓은 영역의 패턴/텍스처 학습

#### Encoder-Decoder 구조
- 이미지를 압축(encode) → 마스크 영역 생성 → 복원(decode)

#### Adversarial Training (GAN)
- 생성된 이미지가 "진짜"처럼 보이도록 학습

### 5.3. 장점과 한계
| 장점 | 한계 |
|------|------|
| 복잡한 텍스처/패턴도 자연스럽게 생성 | 학습 데이터에 없는 패턴은 잘 못함 |
| 큰 영역도 처리 가능 | GPU 필요 |
| 의미론적 이해 가능 | 해상도 제한 (512x512) |

---

## 6. 세 가지 방법 비교

### 6.1. 성능 비교표
| 방법 | 속도 | 품질 | 적합한 상황 |
|------|------|------|-------------|
| 수업 기법 (Class) | 빠름 | 낮음 | 단조로운 배경 |
| Telea (직접 구현) | 중간 | 중간 | 에지 보존 필요 |
| LaMa (딥러닝) | 느림 | 높음 | 복잡한 배경 |

### 6.2. 왜 세 가지를 모두 구현했는가?
- 수업에서 배운 기법의 한계를 직접 체험하기 위해
- 전통적 알고리즘(Telea)과 딥러닝(LaMa)의 차이를 이해하기 위해
- 인페인팅 분야의 발전 과정을 탐구하기 위해

---

## 7. 결론

### 7.1. 배운 점
- 수업 기법만으로도 간단한 케이스는 해결 가능
- 복잡한 케이스는 더 정교한 알고리즘(Telea) 또는 딥러닝(LaMa) 필요
- 각 알고리즘의 원리를 이해하고 적절히 선택하는 것이 중요

### 7.2. 데모 시연
- 실제 이미지에서 세 가지 모드 비교 시연
